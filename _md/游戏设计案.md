# 游戏设计案 —— 3D 网格推箱解谜

| 项目 | 内容 |
|---|---|
| 项目代号 | Sokoban3D（暂定） |
| 文档类型 | 游戏设计案（GDD） |
| 引擎 | Unity 2022.3.51f1 |
| 平台 | PC（键鼠） |
| 视角 | 3D 俯视 / 半俯视 |
| 玩法类型 | 3D 网格推箱解谜（Grid-based Puzzle） |
| 人数 | 单机 |
| 关卡结构 | 4 章，共 25 关（3 / 5 / 5 / 12） |
| 版本 | v0.4（相机相对方向输入、点击寻路、坡度语义修正） |
| 关联文档 | 《关卡编辑器设计案》 |

---

## 1. 概述

### 1.1 一句话定位

一款基于整数网格、**纯逻辑零运气**的 3D 推箱解谜游戏。以**章节递进**的方式逐步引入机制：从经典推箱，到彩色按钮开门，到多角色切换协同，再到带层高与坡度的立体推箱。

### 1.2 核心体验

> "我只能推，不能拉；所以每一步都要先想清楚后三步。"

玩家面对的是一个**不可逆**的世界。乐趣不来自操作，而来自**前瞻规划、死锁识别与子问题分解**。

### 1.3 目标用户

喜欢纯逻辑解谜、空间推理、规划类玩法的玩家（推箱子、Baba Is You、Cosmic Express、Sokoban 社区用户）。

---

## 2. 设计支柱（Design Pillars）

1. **纯逻辑，零运气，零反应**
   无时间压力、无随机数、无操作精度要求。失败只能归因于思考不足。
2. **不可逆性带来重量**
   "只能推不能拉"是难度根源。撤销与重开必须廉价，但决策本身要有分量。
3. **机制逐章递进**
   每一章只引入一个新变量，先教学后组合，保证难度曲线平滑。
4. **机制可插拔**
   所有关卡元素（按钮、颜色、坡度等）必须能由关卡编辑器无代码扩充。

---

## 3. 章节总览

| 章 | 主题 | 关卡数 | 新增变量 | 出生点数 m | 箱子数 n |
|---|---|---|---|---|---|
| 第一章 | 经典推箱 | 3 | 基础推箱规则 | 1 | n |
| 第二章 | 彩色按钮 | 5 | 彩色按钮 + 彩色可解锁墙 | 1 | n |
| 第三章 | 多角色切换 | 5 | m 个出生点 / Tab 切换 / 人物实体 | 2~3 | n |
| 第四章 | 坡度与层高 | 12 | 坡度墙 / 坡度箱 / 重力 / 层高 | 1~3 | n |
| 合计 | | **25** | | | |

> **章节累积**：后一章保留前面所有机制。第四关同时可能出现按钮、多角色与坡度。

---

## 4. 世界与网格定义

### 4.1 网格化世界

- 世界由**整数网格**构成，所有可交互对象严格吸附在格子上。
- 主平面为 **XZ 平面**，Y 轴表示**层高（层数）**。
- 单格边长（cellSize）：默认 `1.0` 世界单位（可配置）。
- 格坐标：`Vector3Int (x, y, z)`；**第一至三章固定 y = 0**，第四章启用 y。

### 4.2 每格的分层结构

每格由多个**可独立存在**的层组成，支撑"墙 / 箱子 / 抵达点共存"：

| 层 | 说明 | 取值示例 |
|---|---|---|
| 地形层 Terrain | 决定这一格能否站立 | 虚空、地板、墙 |
| 占据层 Occupant | 站在格子上的可移动物体 | 无、箱子 |
| 标记层 Marker | 静态关卡标记 | 无、抵达点、出生点、按钮 |

> 例：一个"墙 + 无占据"的格；一个"地板 + 箱子 + 抵达点"的格（箱子已就位）。

### 4.3 第四章：地形统一为"实体块 + 层高"

第四章的核心设定：**地板和墙是同一种物体**，区别只在**它占据哪一层**以及**上面是否还有空间**。

- 一个**实体块**占据某个格子的某一层。
- 实体块**上方若有空间**，则该顶面可作为**地板**供人行走。
- 实体块**高于相邻可行走面的那部分**，表现为**墙**（阻挡）。
- 因此"墙"与"地板"不是两种资产类型，而是**同一实体块在不同层高关系下的表现**。

> 编辑器侧对应：绘制的是**层高（高度场）**，不是"墙/地板"二选一。

### 4.4 3D 表现原则

- 逻辑是网格数据，**表现是 3D**：地板、墙、箱子、角色、坡均为 3D 模型。
- 逻辑位置与表现位置解耦：逻辑瞬变，表现为插值动画。
- 相机：半俯视，跟随当前操控角色，支持旋转/缩放（见 §9）。

---

## 5. 移动规则（通用）

### 5.1 移动模型：纯格步进

- 角色每次移动**跨越恰好一个格子**。
- 按一次走一格；持续按住则固定间隔**连续步进**。
- 表现插值（建议 `0.12s` / 格），但**逻辑在按键瞬间结算**。
- 禁止自由移动 / 惯性 / 加减速，保证谜题确定性。

### 5.2 推箱三原则（经典规则，全程有效）

1. **只能推，不能拉**
2. **一次只能推一个箱子**（箱子后方必须是空且可进入的格）
3. **推的前提**：目标格可进入，且箱子后方格可进入且无占据物

> FAQ：经典推箱子**不能**一次推动两个箱子。箱子后方若有箱、墙、角色，推动一律失败。

### 5.3 移动结算优先级

```
目标格不可进入（墙/虚空/实体）   → 拒绝，角色不动
目标格是箱子：
    箱子后方不可进入             → 拒绝，角色不动
    箱子后方可进入               → 箱子先移动一格，角色进入原箱格
目标格为空                       → 角色进入
```

### 5.4 死锁（Deadlock）

死锁 = 箱子进入**永远无法再被推动**且不在抵达点上的状态。

本作中死锁**不属于规则禁止项**，而是玩家需自行识别的风险，系统仅提供**软提示**：

- **静态死格**：预计算的、推入必死的格子（墙角、贴墙非目标格）
- **动态死锁**：2×2 箱子方阵、相互冻结的箱子组
- 表现：箱子/格子变灰提示，不弹窗、不禁操作

> 设计意图：保留自主性与"我早该看出来"的顿悟感。

---

## 6. 机制系统

### 6.1 机制三要素

```
触发器 Trigger  --连线-->  动作 Action  --作用于-->  目标 Target
（如：按钮被按下）        （如：解锁）              （如：某颜色可解锁墙）
```

### 6.2 第二章：彩色按钮 + 彩色可解锁墙

| 项 | 规则 |
|---|---|
| 按钮颜色 | 每种按钮有颜色标识（红 / 蓝 / 黄 / 绿…，可扩充） |
| 触发方式 | 被**箱子或角色占据**时按下 |
| 作用目标 | 同颜色的「可解锁墙」 |
| 作用效果 | 目标墙 **变为地板**（原地保留，从阻挡物变为可通行地面） |
| 持续时间 | **永久解锁**（按下即生效，离开不复原） |

设计要点：
- 同一颜色可有多个按钮、多块墙，形成"一处触发、多处开路"。
- 按钮颜色与墙颜色的匹配关系**由编辑器可视化连线**，不写死。
- "变地板"而非"消失"，意味着原有墙体体积仍在，可用于多层结构（第四章）。

> **已定稿**：按钮为**永久解锁**（按下即生效，离开不复原）。若后续需要"需持续压住"的变体，作为新按钮类型扩充。

### 6.3 第三章：多角色切换

| 项 | 规则 |
|---|---|
| 出生点 | 关卡含 **m 个出生点**（本章 m = 2 或 3），生成 m 个玩家 |
| 操控 | 同一时刻只有一个玩家响应 WASD |
| 切换 | `Tab` 循环切换当前操控角色 |
| 切换成本 | **不消耗步数、不消耗时间**，任意时刻可用 |
| 人物实体性 | 角色是**实体**，占据格子，**不可被穿过、不可被推动** |
| 阻塞 | 角色若不主动撤离，会像墙一样**造成堵塞**（箱子推不动、其他角色过不去） |
| 相机 | 跟随当前激活角色 |

设计要点：
- 双角色形成"一个角色牺牲自己开路 / 保持站位，另一个通过"的经典结构。
- 角色实体性既是资源也是障碍：站位本身是一种可消耗/可回收的"工具"。
- 切换零成本，鼓励自由推演。

> **待确认**：角色之间能否踩在对方头上？（暂定：**不能**，角色是不可堆叠实体，仅第四章起箱子可被踩。）

### 6.4 机制求解（设计约束）

- 机制在**每次世界状态变化后**重新求解，直至稳定（链式联动一次算清）。
- 必须保证：**结果只依赖当前世界状态，不依赖求解顺序**，避免歧义。
- 出现无解震荡（A 开 B、B 关 A）视为**关卡设计错误**，由编辑器校验拦截。

---

## 7. 第四章：层高、坡度与重力

### 7.1 层与行走（体素模型）

- 世界为**体素网格**，每格层高**理论上无上限**（可堆叠任意层）。
- 角色占据一个体素 `(x, y, z)`；站立条件：该体素为空，且**正下方为实体块**。
- 一次移动的判定只看**当前高度**：目标格在**当前层无阻碍**即可进入（有实体块则该方向被阻挡，即表现为墙）。
- 高度差通过**坡度**跨越（见 §7.2）；没有坡度就无法上到更高层。

### 7.2 坡度（统一几何规则）

- **所有坡都是 1/2 正方体**（楔形），占用一格，并带一个**攀爬方向**（4 方向之一，编辑器可旋转）。
- 本质接近**"带方向的可攀爬墙"**：**没有滑动**，站上去稳定，也**不会自动前进到下一格**。
- 进入坡格的规则：

| 进入方向 | 结果 |
|---|---|
| 沿**攀爬方向**进入 | **上坡**：站上该坡格本身，高度 **+1**（停在坡格，不越格） |
| 其余 **3 个方向**进入 | 坡度箱 → 视作**常规箱子**推动；坡度墙 → 视作**固定障碍**，不可进入 |

- 例：坡位于 `(x+1, z)`、攀爬方向 `+x`。玩家从 `(x, z)` 向 `+x` 走 → 落在坡格 `(x+1, y+1)`，**不会再前进到 `(x+2)`**。
- 由于只站到坡格本身，**坡后方是否虚空 / 是否为更高的围墙均不影响上坡**——紧邻虚空的坡同样可以攀爬。
- 从坡格继续移动时，按常规规则（含重力）判定目标格。

### 7.3 坡度墙 vs 坡度箱

- **坡度墙**：固定地形，不可推动；3 个非上升面均视为墙。
- **坡度箱**：可推动物体，朝向固定；**攀爬方向面不可推**（进入即上坡），其余 **3 面按常规箱子推动**。
- 二者共用同一套几何与上坡规则，仅"是否可推"不同。

### 7.4 重力（同一步内结算）

- 重力**在一步内结算**：一次输入 = 一次完整结算 = **一个可撤销的步**。
- 结算顺序：

```
移动判定（当前层是否有阻碍）
  └ 可进入 → 到达目标体素
       └ 检查下方是否踩实
            ├ 踩实   → 结束
            └ 未踩实 → 下落，直到落到支撑面（或停在阻挡物之上）
```

- 角色与箱子遵循**同一套重力**。
- 箱子被从上层推下 / 推上坡时，同样掉落。

### 7.5 重量与堆叠

- **可推动重量上限 = 1**。
- 单个箱子（重量 1）可推。
- **箱子叠箱子**、**箱子上站人** → 重量 ≥ 2 → **不可推动**。
- 箱子**允许堆叠**（可垒高作为平台）。
- **不可推动承载自己的箱子**（脚下方）。
- 结论：不存在"站在箱子上把箱子推走"的情况，规则上天然禁止。

### 7.6 虚空

- 虚空是**绝对空气墙**：不可进入、不可穿越、不可从中掉落。
- 虚空的坡**仍可攀爬**（坡只把人抬到坡格本身，不依赖坡后方有落脚点）。

### 7.7 第四章交互补充

- 箱子成为**可垫高的可踩平台**，堆叠用于抵达高处。
- 推动、站立、下落的**顺序依赖**是第四章主要难度来源。
- 多层结构（二层、三层乃至更高）用于构建上下联动的解谜空间。

---

## 8. 关卡元素总表（可扩充）

所有元素遵循 **分类（Category）+ 类型（Type）** 两级结构，类型 ID 使用**字符串**以便无痛扩充。

| 类型 | 分类 | 引入章 | 说明 |
|---|---|---|---|
| 虚空 | 地形 | 1 | 绝对空气墙，不可进入 |
| 地板 | 地形 | 1 | 基础可通行地面 |
| 墙 | 地形 | 1 | 阻挡物 |
| 箱子 | 占据 | 1 | 可推动的核心物件 |
| 抵达点 | 标记 | 1 | 箱子需要被推上去的目标 |
| 出生点 | 标记 | 1 | 角色初始位置（绑定角色序号） |
| 彩色按钮 | 机制 | 2 | 被占据时按下，解锁同色墙 |
| 可解锁墙 | 机制 | 2 | 被同色按钮触发后变为地板 |
| 坡度墙 | 地形 | 4 | 固定斜面，可上 1 层 |
| 坡度箱 | 占据 | 4 | 可推动斜面物，受重力 |
| （预留）冰面 / 钥匙 / 传送门 | — | — | 由编辑器新增类型即可 |

> 新增元素（冰面、钥匙、传送门等）时，**只需新增类型定义，不改动框架代码**。详见《关卡编辑器设计案》。

---

## 9. 操作与相机

### 9.1 键位（默认，可配置）

| 操作 | 键位 |
|---|---|
| 移动 | `W` `A` `S` `D`（相机相对） |
| 点击寻路 | 鼠标左键 |
| 切换角色 | `Tab`（第三章起） |
| 撤销一步 | `R` |
| 重置本关 | `P` |
| 相机旋转 | `Q` / `E`（或鼠标右键拖动） |
| 相机缩放 | 鼠标滚轮 |
| 暂停/菜单 | `Esc` |

### 9.2 方向输入与相机对齐（正交四方向）

- 世界移动恒为 **4 个正交方向**（`+x` / `-x` / `+z` / `-z`），**不存在斜向移动**。
- **默认视角**：俯角 **60°**，看向 `+z`。此时 `+z` 为"正方向"：
  - `W → +z`，`S → -z`，`D → +x`，`A → -x`
- **相机旋转中（按住 `Q` / `E`）**：**禁止 WASD 输入**。
- **松开的一瞬间**：视角固定，从 `+x / -x / +z / -z` 中选出**与当前视角朝向最接近**的一个，作为新的"正方向"。
  - `W` → 正方向
  - `S` → 反方向
  - `D` → 正方向顺时针 90°
  - `A` → 正方向逆时针 90°
- 方向对齐是**离散吸附（四选一）**，保证输入永远映射到 4 个正交方向之一。

### 9.3 点击寻路（自动行走）

- **鼠标左键点击场景中的可站立面**（即实体块顶面 = 地板），选中目标格。
- 系统在网格上搜索路径，控制**当前激活角色**自动逐格走过去。
- **自动行走期间不可推箱子**：路径必须绕开所有箱子 / 障碍格。
  - 若**不存在**"不推箱也能到达"的路径 → 本次点击**不移动**（可给提示）。
- 自动行走中按任意移动键或再次点击 → **立即取消**。
- 路径搜索：网格 BFS（四方向，保证最短）；箱子、墙、其他角色（第三章）均视为障碍。

### 9.4 撤销与重置（核心辅助）

- **逐步撤销（`R`）**：每按一次回退**一步**，可连续回退到关卡初始状态。
- **重置本关（`P`）**：一键回到关卡初始状态。
- 二者必须**免费且即时**，是游戏的基本盘，不是附加功能。

### 9.5 相机

- 半俯视，**默认俯角 60°，看向 `+z`**。
- 自动跟随当前激活角色，平滑插值。
- 支持手动旋转（`Q` / `E`）与缩放，便于观察第四章多层结构。
- 旋转结束后按 §9.2 重新确立正方向。
- 切换角色时相机平滑过渡，不瞬移。

---

## 10. 反馈与表现

- **移动表现**：角色 / 箱子移动插值（0.12s / 格）；逻辑瞬时、表现平滑。
- **可推行提示**：角色面向的可推箱子轻微高亮。
- **死锁提示**：进入死格的箱子变灰（软提示）。
- **按钮反馈**：按下时按钮及同色墙同步高亮，解锁瞬间有明显的通路面变化。
- **抵达反馈**：箱子推上抵达点时有明确视觉 / 音效确认。
- **重力反馈**：下落有轻微加速与落地反馈。
- 音效与特效：移动、推箱、抵达、解锁、下落各一套（占位待补）。

---

## 11. 关卡目标与通关条件

### 11.1 通关条件

- **基础条件**：所有「抵达点」都被「箱子」覆盖。
- 关卡非法情形（抵达点数 ≠ 箱子数等）由编辑器校验拦截。

### 11.2 可选附加条件（后续扩展）

- 指定角色必须到达指定标记点
- 限制最大步数 / 推数（挑战模式）
- 按钮必须保持激活状态通关

### 11.3 章节关卡编制规范

| 章 | 出生点 | 箱子/抵达点 | 机制约束 |
|---|---|---|---|
| 第一章 | 1 | n : n | 纯推箱，无机制 |
| 第二章 | 1 | n : n | 出现彩色按钮与同色可解锁墙，必须可解且不依赖顺序歧义 |
| 第三章 | 2 或 3 | n : n | 角色实体阻塞；可利用角色站位作为解法的一部分 |
| 第四章 | 1~3 | n : n（含坡度箱） | 层高 / 坡度 / 重力，注意落点与支撑 |

### 11.4 评价体系（后续）

- 三星制：以步数 / 推数相对最优解的比值划分（最优解由离线求解器提供）。

---

## 12. 难度与关卡结构

### 12.1 难度代理指标

| 指标 | 难度关系 |
|---|---|
| 箱子数量 | ↑ 难 |
| 地图尺寸 | ↑ 难 |
| 最优推数 / 步数 | ↑ 难 |
| 解的分支度（可行解法数量） | ↓ 难 |
| 死锁陷阱数量 | ↑ 难 |
| 箱子依赖链深度 | ↑ 难 |
| 角色数量 / 协同需求 | ↑ 难（第三章） |
| 层数与坡度数量 | ↑ 难（第四章） |

### 12.2 难度曲线原则

- 新机制先出**单一变量教学关**，再出组合关。
- 严禁难度断崖：新机制引入后至少 2 关用于巩固。
- 单关元素数量递增。

---

## 13. 内容规划（25 关草案）

### 第一章 · 经典推箱（3 关）

| 关 | 主题 | 教学点 |
|---|---|---|
| 1-1 | 第一步 | WASD 移动、推箱、抵达点 |
| 1-2 | 只能推不能拉 | 不可逆性的体感 |
| 1-3 | 狭窄通道 | 顺序与拥堵、死锁初识 |

### 第二章 · 彩色按钮（5 关）

| 关 | 主题 | 教学点 |
|---|---|---|
| 2-1 | 按下按钮 | 按钮与同色墙的对应关系 |
| 2-2 | 两个颜色 | 多颜色分离触发 |
| 2-3 | 先开门后过路 | 顺序依赖 |
| 2-4 | 按钮与箱子共用通道 | 资源冲突 |
| 2-5 | 综合 | 多色 + 多箱 + 抵达 |

### 第三章 · 多角色切换（5 关）

| 关 | 主题 | 教学点 |
|---|---|---|
| 3-1 | 两个角色 | Tab 切换 |
| 3-2 | 站位即阻塞 | 角色实体性 |
| 3-3 | 一个角色开路 | 角色作为工具 |
| 3-4 | 三角协同 | m = 3 |
| 3-5 | 角色 + 按钮 | 与第二章机制组合 |

### 第四章 · 坡度与层高（12 关）

| 关 | 主题 | 教学点 |
|---|---|---|
| 4-1 | 认识坡度 | 借助坡度上二层 |
| 4-2 | 落差与掉落 | 走下 / 掉下与重力 |
| 4-3 | 踩箱子 | 箱子作为可踩平台 |
| 4-4 | 推箱下坡 | 箱子被推下坡的重力运动 |
| 4-5 | 坡度箱初识 | 从斜面推、上坡到坡后 |
| 4-6 | 坡后无物块 | 掉落到下层 |
| 4-7 | 多坡联动 | 上下层路径规划 |
| 4-8 | 多层按钮 | 层高 + 彩色按钮 |
| 4-9 | 双角色 + 层高 | 一人留守一人上下 |
| 4-10 | 堆叠与落差 | 箱子堆叠垫高 |
| 4-11 | 综合Ⅰ | 坡度 + 按钮 + 箱子 |
| 4-12 | 综合Ⅱ | 全要素终章 |

---

## 14. 数值与配置（初值）

| 参数 | 初值 | 说明 |
|---|---|---|
| 单格边长 cellSize | 1.0 | 世界单位 |
| 移动时长 | 0.12s / 格 | 表现插值 |
| 长按连续步进间隔 | 0.15s | 输入重复 |
| 下落加速度 | 待定 | 重力表现 |
| 相机俯角 | 60° | 默认视角，看向 `+z` |
| 相机跟随平滑 | 0.15s | 阻尼时间 |
| 相机缩放范围 | 5 ~ 25 | 正交尺寸 / 距离 |
| 层高上限 | 理论无限 | 第四章 |

---

## 15. 与编辑器 / 技术接口约定

（技术策划视角，保证设计与工具自洽）

- 关卡**唯一数据源**为 JSON，由《关卡编辑器》产出，运行时只读取。
- 世界中每个对象都是「类型 ID + 格坐标 + 参数」；类型定义由**类型注册表**统一管理。
- 第四章的层高需在数据层以**高度场 / 多层体素**表达，编辑器应支持**分层绘制**。
- 彩色按钮与墙的对应、机制连线，全部**可视化配置**，不写死在代码。
- 新增元素 / 机制：**只新增类型定义与机制实现，不改工具与框架核心**。
- 详细数据结构、分层规范与扩充机制见《关卡编辑器设计案》。

---

## 16. 里程碑（初步）

| 阶段 | 目标 |
|---|---|
| M1 | 运行时最小闭环：单角色 + 推箱 + 抵达判定 + 撤销/重置 |
| M2 | 关卡编辑器 v1：调色板 + 场景绘制 + JSON 存取 + 校验 |
| M3 | 第二章机制：彩色按钮 + 可解锁墙 + 连线 + 求解 |
| M4 | 第三章：多角色切换 + 实体阻塞 + 相机 |
| M5 | 第四章：层高 + 坡度 + 重力 + 踩箱 |
| M6 | 内容：25 关教学与进阶关卡 |

---

## 17. 待确认项与风险

| 项 | 说明 | 状态 |
|---|---|---|
| 角色是否可互相踩 | 暂定不可（仅箱子 / 坡可踩） | 待确认 |
| 坡度箱顶面站立 / 相互堆叠 | 半立方体顶面是否可作为平台 | 待确认 |
| 下落路径碰撞细节 | 下层已有箱子 / 角色时的落点处理 | 待确认（默认停在阻挡之上） |
| 点击寻路的撤销粒度 | 每一步一撤 vs 整段一撤 | 待确认 |
| 点击寻路不可达时的反馈 | 不移动 vs 给提示 | 待确认 |
| 使用求解器验证可解性 | 需引入离线 A*/IDA* 求解器 | 待评估 |
| 评价体系（星级） | 依赖求解器提供最优解 | 待 M6 后 |
| 撤销实现方式 | 原型期快照，后期改命令栈 | 待优化 |
| 第四章求解器 | 带层高/坡度的求解复杂度高 | 高风险 |
| 正式美术资源 | 原型期全部占位 | 待定 |

---

## 附 A：经典推箱子规则 FAQ

| 问题 | 答案 |
|---|---|
| 能一次推两个箱子吗？ | **不能**。箱子后方必须是空地，连推属于魔改规则。 |
| 能把箱子从墙上拉回来吗？ | **不能**。只有推，没有拉。 |
| 箱子推进墙角还有救吗？ | 没有，属于永久死锁，只能撤销。 |
| 角色能推动箱子到自己脚下吗？ | 不能，角色不能与箱子同格。 |

## 附 B：术语表

| 术语 | 含义 |
|---|---|
| 格 / Cell | 世界的最小逻辑单位 |
| 纯格步进 | 每次移动恰好跨越一格，逻辑瞬时结算 |
| 死锁 Deadlock | 箱子进入永久无法推动且非目标的状态 |
| 静态死格 Dead Square | 预计算的、推进去必死的格子 |
| 层高 / 高度场 | 第四章中每格堆叠的实体块数量 |
| 坡度墙 | 固定斜面地形，可上 1 层 |
| 坡度箱 | 可推动斜面物体，受重力 |
| 机制连线 Link | 触发器 → 动作 → 目标 的关联关系 |
| 类型注册表 | 集中管理所有关卡元素定义的资产 |
